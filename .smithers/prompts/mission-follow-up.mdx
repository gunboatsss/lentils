# Mission Follow-Up

Validation found issues in the current milestone. Fix the concrete regressions and gaps before the mission proceeds.
Keep changes targeted. Do not begin the next milestone.

Never run scripts/install-symlinks.sh. It is a manual, human-only system installation tool. Use `lake build` to verify fixes and run tests/sandbox scripts for validation instead.

MISSION GOAL:
{props.missionGoal}

MILESTONE:
```json
{JSON.stringify(props.milestone, null, 2)}
```

VALIDATION RESULT:
```json
{JSON.stringify(props.validation, null, 2)}
```

REQUIRED OUTPUT:
{props.schema}

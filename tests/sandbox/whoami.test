#!/bin/bash
# whoami.test — Tests for the `whoami` utility
# License: 0BSD
# Run via: run-sandbox-tests.sh whoami

[ -f testing.sh ] && . testing.sh

# Basic output (differential against /bin/whoami)
testing "whoami" \
  '"$UTIL"' \
  "" "" ""

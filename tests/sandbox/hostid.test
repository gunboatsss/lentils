#!/bin/bash
# hostid.test — Tests for the `hostid` utility
# License: 0BSD
# Run via: run-sandbox-tests.sh hostid

[ -f testing.sh ] && . testing.sh

# Basic hostid output (differential against /bin/hostid)
testing "hostid" \
  '"$UTIL"' \
  "" "" ""

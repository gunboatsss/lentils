#!/bin/bash
# uname.test — Tests for the `uname` utility
# License: 0BSD
# Run via: run-sandbox-tests.sh uname

[ -f testing.sh ] && . testing.sh

# Basic output (differential against /bin/uname)
testing "uname" \
  '"$UTIL"' \
  "" "" ""
